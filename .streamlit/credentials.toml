[general]
# Streamlit's first-run onboarding asks for an email at an interactive
# terminal that doesn't exist on Streamlit Cloud — that's why the app
# hangs at startup. Setting an empty email here skips the prompt entirely
# so the app boots cleanly.
email = ""
