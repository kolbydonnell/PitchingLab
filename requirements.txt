# Version specs use >= (not ==) so pip can find prebuilt wheels for
# whichever Python version you're on. Streamlit Cloud Python version
# is pinned to 3.12 via runtime.txt so MediaPipe wheels are available.

streamlit>=1.36.0
pandas>=2.2.0
plotly>=5.22.0
kaleido>=0.2.1    # renders Plotly figs as static PNGs (mobile rotation fix)
pillow>=11.0
reportlab>=4.0
matplotlib>=3.9
pypdf>=4.0

# =====================================================================
# Live Capture + Upload Video — phone/tablet camera tracking
# =====================================================================
# av needs ffmpeg from packages.txt (already added).
streamlit-webrtc>=0.47.0
opencv-python-headless>=4.10.0
av>=12.0
numpy>=1.26

# Pose extraction via MediaPipe — drives skeleton overlay + biomech in
# Live Capture and Upload Video. Wheels available for Python 3.9-3.12, so
# Streamlit Cloud must run 3.12 (pinned in runtime.txt).
mediapipe>=0.10.14
