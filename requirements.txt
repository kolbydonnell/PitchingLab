# Version specs use >= (not ==) so pip can find prebuilt wheels for
# whichever Python version you're on (3.10, 3.12, 3.13, 3.14, etc.).
# Pinning with == forces pip to build from source when a wheel doesn't
# exist for your Python — that's what causes "failed wheel build" errors.

streamlit>=1.36.0
pandas>=2.2.0
plotly>=5.22.0
pillow>=11.0      # imaging — Python 3.14 wheels start here
reportlab>=4.0
matplotlib>=3.9
pypdf>=4.0

# =====================================================================
# Live Capture (Beta) — phone/tablet camera tracking
# =====================================================================
# These are DISABLED for Streamlit Cloud because `av` (PyAV) needs
# system-level FFmpeg libraries that the default Cloud container doesn't
# ship. The rest of the app (Pitching Lab, Hitting Lab, Tunneling,
# Reports, PDFs) deploys cleanly without them. The Live Capture tab in
# the cloud version will show a "package missing" message — that's
# expected.
#
# For LOCAL use (your Mac), uncomment these four lines, run
#   pip3 install -r requirements.txt --upgrade
# and Live Capture works the same as before.
#
# To re-enable Live Capture on a hosted version of the app, deploy to a
# host that supports custom Dockerfiles (Render, Railway, fly.io —
# each has a free tier).
#
# streamlit-webrtc>=0.47.0
# opencv-python-headless>=4.10.0
# av>=12.0
# numpy>=1.26

# OPTIONAL — pose extraction via MediaPipe.
# MediaPipe ships wheels for Python 3.9-3.12. If you enable Live Capture
# above and you're on Python 3.9-3.12, you can also uncomment this line:
# mediapipe>=0.10.14
