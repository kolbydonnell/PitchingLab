# Version specs use >= (not ==) so pip can find prebuilt wheels for
# whichever Python version you're on. Streamlit Cloud Python version
# is pinned to 3.12 via runtime.txt so MediaPipe wheels are available.

streamlit>=1.36.0
pandas>=2.2.0
plotly>=5.22.0
kaleido>=0.2.1    # renders Plotly figs as static PNGs (mobile rotation fix)
pillow>=11.0
reportlab>=4.0
matplotlib>=3.9
pypdf>=4.0
stripe>=9.0       # subscription billing (Stripe Checkout + Customer Portal)

# =====================================================================
# Live Capture + Upload Video — phone/tablet camera tracking
# =====================================================================
# av needs ffmpeg from packages.txt (already added).
streamlit-webrtc>=0.47.0
opencv-python-headless>=4.10.0
av>=12.0
numpy>=1.26
streamlit-image-coordinates>=0.1.6   # click-to-calibrate plate

# Pose extraction via MediaPipe — drives skeleton overlay + biomech in
# Live Capture and Upload Video. Wheels available for Python 3.9-3.12.
# 0.10.21 is the most recent stable version in the 0.10.x series before
# the API restructure in 0.10.30+. We chmod the bundled .tflite files
# explicitly in the Dockerfile so the permission issue we hit on
# Streamlit Cloud doesn't apply here.
mediapipe==0.10.21
